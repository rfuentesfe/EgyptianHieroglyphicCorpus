services:
  backend:
    build: ./maat/backend
    ports:
      - "8000:8000"
    volumes:
      - ./maat/outputs:/app/outputs
      - ./maat/models:/app/models

  frontend:
    build: ./maat/frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
