services:
  backend:
    build: ./maat/backend
    ports:
      - "8011:8011"
    volumes:
      - ./maat/outputs:/app/outputs
      - ./maat/models:/app/models

  frontend:
    build: ./maat/frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
